import { report, csv } from './report-core.js';
import { listTransactions } from './finance-core.js';
function euro(n){return Number(n||0).toFixed(2)+' €'}
async function refresh(){const r=await report();const box=document.getElementById('reportValues');if(!box)return;box.innerHTML=`<div>24 h: <b>${euro(r.today.income-r.today.expense)}</b></div><div>7 dana: <b>${euro(r.week.income-r.week.expense)}</b></div><div>Mjesec: <b>${euro(r.month.income-r.month.expense)}</b></div><div>Sve: <b>${euro(r.all.income-r.all.expense)}</b></div>`}
function init(){const card=document.createElement('section');card.className='card';card.innerHTML='<h2>📑 Izvještaji</h2><div id="reportValues" class="list"></div><button id="exportData" class="secondary" style="margin-top:10px">Izvezi financije CSV</button>';document.querySelector('main').insertBefore(card,document.querySelector('#settingsTitle').closest('.card'));document.getElementById('exportData').onclick=async()=>{const rows=await listTransactions();const blob=new Blob([csv(rows)],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='vas-charlie-financije.csv';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};refresh();window.addEventListener('vc-finance-changed',refresh)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
